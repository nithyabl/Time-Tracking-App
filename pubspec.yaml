name: time_tracking_app_new
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_offline: "^4.0.0"
  kiwi: ^5.0.1
  intl: any
  flutter_boardview: ^0.2.1
  http: ^1.2.2
  flutter_bloc: ^8.1.6
  uuid: ^4.5.1
  sqflite: ^2.4.1
  synchronized: ^3.3.0+3
  shared_preferences: ^2.3.3

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.13
  kiwi_generator: ^4.2.1
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
